<%= form_for(@masterservice) do |f| %>
  <% if @masterservice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@masterservice.errors.count, "error") %> prohibited this masterservice from being saved:</h2>

      <ul>
      <% @masterservice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description, class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit nil, class: "btn btn-primary" %>
  </div>
<% end %>
